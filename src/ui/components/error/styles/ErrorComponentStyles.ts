import styled from "styled-components/native";
import { View } from "react-native";

const ErrorContainerStyled = styled(View)`
  flex: 1;
  justify-content: center;
  align-items: center;
`;

export default ErrorContainerStyled;
